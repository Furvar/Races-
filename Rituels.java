package be.Vilevar.Races.Rituels;

public class Rituels/* implements Listener */{

/*	public static boolean enable = true;
	
	@SuppressWarnings("deprecation")
	@EventHandler
	public void onRituel(PlayerInteractEvent e){
		Player player = e.getPlayer();
		if(e.getAction() == Action.LEFT_CLICK_BLOCK && (e.getItem() != null && player.getItemInHand() != null) && e.getItem() == player.getItemInHand()){
			//nain
			if(Raccourcis.isNainRit(player)){
				Raccourcis.setRitNain(player, Raccourcis.getChance(player));
			}
		}
	}*/
}
